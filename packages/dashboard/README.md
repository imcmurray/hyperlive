# dashboard (Phase 4 — not yet built)

Operator console for a live channel:

- **Kill switch** — freeze all mutations and drop to a safe holding scene.
- Live view of the moderation queue + audit log (comment → decision → mutation).
- Stream health (ffmpeg up/restarts, encode CPU, dropped frames).
- Manual override: post any directive, ban a user, force a theme/scene.
